const timeago = require('timeago.js');

// const timeagoInstance = new timeago();

const helpers = {};

// helpers.timeage = (savedTimestamp) => {
//     return timeagoInstance.format(savedTimestamp);
// }

module.exports = helpers;
